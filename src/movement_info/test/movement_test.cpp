#include "gtest/gtest.h"
#include "gmock/gmock.h"

TEST(MovementTest, publish_movement_info)
{
    EXPECT_EQ(1, 0);
}

int main(int argc, char **argv)
{
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}